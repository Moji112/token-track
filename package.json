{
  "name": "trailhead-token-bot",
  "version": "1.0.0",
  "description": "Trailhead — live coin/NFT discovery tracker with configurable Discord liquidity alerts.",
  "main": "server/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --watch --env-file-if-exists=.env server/index.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^4.19.2"
  }
}
