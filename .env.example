# Copy this file to .env and fill in your own values. .env is gitignored —
# never commit real secrets.

# Optional. Set this to require a password (any username, this password) to
# open the app or call its API. Leave unset to run without a login prompt.
TRAILHEAD_PASSWORD=

# Optional. Port the server listens on. Defaults to 3000.
PORT=3000
